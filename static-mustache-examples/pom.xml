<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.github.sviperll</groupId>
        <artifactId>static-mustache-maven-parent</artifactId>
        <version>0.3-successor-SNAPSHOT</version>
    </parent>
    <artifactId>static-mustache-examples</artifactId>
    <name>static-mustache-examples</name>
    <packaging>jar</packaging>
    <dependencies>
        <dependency>
            <groupId>com.github.sviperll</groupId>
            <artifactId>static-mustache</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.sviperll</groupId>
            <artifactId>chicory-text</artifactId>
            <version>0.26</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <configuration>
                    <!--
                         This dependencies are used at compile time only.
                         maven-dependency-plugin sees runtime dependencies only,
                         so it consideres dependencies to be unused
                      -->
                    <ignoredUnusedDeclaredDependencies>
                        <ignoredUnusedDeclaredDependency>com.github.sviperll:static-mustache:*:*</ignoredUnusedDeclaredDependency>
                    </ignoredUnusedDeclaredDependencies>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>

