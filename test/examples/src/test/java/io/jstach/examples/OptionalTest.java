package io.jstach.examples;

import org.junit.Test;

public class OptionalTest {

	@Test
	public void testName() throws Exception {
		// OptionalContainer child = new OptionalContainer(Optional.empty(), true,
		// Optional.empty());
		//
		// OptionalContainer oc = new OptionalContainer(Optional.of("blah"), false,
		// Optional.of(child));
		//
		// String actual = JStachio.render(oc);
		//
		// assertEquals("", actual);
	}

}
